# 🏀 Basketball Tracking Analytics

Полностью автономное (офлайн) desktop-приложение на Python с графическим
интерфейсом [Streamlit](https://streamlit.io/) для аналитики баскетбольных
тренировок по видео со статичной камеры.

Приложение детектирует и трекает игроков и мяч моделью **YOLO11x**
(Ultralytics) с трекером **BoT-SORT + ReID**, математически определяет
броски/попадания в кольцо и передачи между игроками, автоматически нарезает
короткие MP4-хайлайты этих моментов и показывает итоговую статистику
(Box Score) прямо в интерфейсе.

Игроки в тренировочной форме **без номеров на майках** — идентификация и
удержание ID при трекинге строится на визуальных признаках (цвет формы,
обуви и т.д.) через модуль ReID трекера, а не на распознавании номеров.

## Возможности

- Пошаговый мастер из 4 шагов: загрузка видео → превью и настройка зон/порогов
  → сопоставление игроков (ID → имя/номер) → полный анализ и статистика.
- Настройка **двух** зон колец кликом мышки прямо по превью-кадру (через
  `streamlit-image-coordinates`) — либо точная донастройка числовыми полями,
  если пакет недоступен.
- Трекинг игроков (`person`) и мяча (`sports ball`) моделью YOLO11x на GPU
  (CUDA) с трекером BoT-SORT, настроенным на ReID по внешнему виду.
- **Авто-калибровка порога владения мячом** под масштаб конкретного видео —
  порог в пикселях по умолчанию считается из среднего размера рамки игрока на
  превью-кадре (а не берётся фиксированным), плюс наглядный круг-подсказка
  этого порога прямо на превью в реальном масштабе кадра.
- Пороги владения мячом, окно передачи, «память» мяча при потере детекции и
  кулдаун события у кольца — всё настраивается слайдерами в GUI, без правки
  кода/yaml.
- Опциональное улучшение качества кадра перед детекцией (апскейл +
  лёгкая резкость) — может помочь трекеру на видео низкого разрешения;
  применяется одинаково и на предварительном сканировании (шаг 3), и в
  финальном анализе (шаг 4).
- **Раздельные пороги уверенности для мяча и игроков** (мяч — маленький и
  часто смазанный объект, его типичная уверенность заметно ниже, чем у
  игроков) плюс настраиваемое разрешение инференса (`imgsz`) — выше
  разрешение помогает модели разглядеть мелкий мяч ценой скорости обработки.
- **Диагностика видимости мяча** (шаг 3) — сэмплирует кадры по всему видео и
  показывает % кадров, где мяч вообще обнаружен, и среднюю уверенность, чтобы
  сразу понять, дело в порогах или модель в принципе не видит мяч на этом
  видео (качество/освещение/дистанция).
- **Экспериментальный режим «камера в движении» (панорама)** — если камера не
  статична, приложение оценивает покадровый сдвиг камеры оптическим потоком и
  автоматически пересчитывает позиции обеих зон колец под текущий кадр.
- Математическая детекция бросков/попаданий с кулдауном (защита от повторной
  фиксации одного и того же события) у каждого из двух колец.
- Детекция передач по смене владельца мяча в настраиваемом временном окне, с
  устойчивостью к коротким пропускам детекции мяча.
- Отладочный таймлайн владения мячом (expander на шаге 4) — видно каждую
  смену владельца, засчитана ли передача, и почему, если нет.
- Ручное сопоставление ID игрока → Имя/Номер по компактной сетке кропов с
  предварительного сканирования (номера на форме не видны камере и не
  распознаются автоматически).
- Автоматическая нарезка автономных MP4-хайлайтов: 5 секунд «до» события
  (кольцевой буфер в ОЗУ) + 2 секунды «после» (следующие кадры видео).
- Автоматическая перекодировка выходного видео и хайлайтов в H.264 через
  ffmpeg (если он установлен), чтобы гарантированно проигрывалось во
  встроенном плеере браузера — с кнопкой скачивания как запасным вариантом.
- Итоговая таблица статистики по игрокам (с именем/номером) и встроенный
  просмотр всех хайлайтов без выхода из приложения.
- Работает полностью офлайн после первого запуска (когда веса модели уже
  скачаны и закэшированы локально).

## Структура репозитория

```
.
├── app.py                  # Всё приложение (Streamlit GUI + логика трекинга/аналитики)
├── requirements.txt        # Python-зависимости с версиями
├── custom_botsort.yaml     # Конфиг трекера (генерируется автоматически при первом запуске)
├── highlights/             # Нарезанные MP4-хайлайты бросков и передач (авто-создаётся)
├── output_videos/          # Полные аннотированные видео обработанных тренировок (авто-создаётся)
└── README.md
```

`custom_botsort.yaml`, `highlights/` и `output_videos/` **не нужно создавать
руками** — `app.py` создаёт их автоматически при первом запуске, если их нет.

## Требования к железу и ПО

- Python 3.10–3.12.
- Рекомендуется NVIDIA GPU с CUDA (протестировано для **RTX 4080**). Без GPU
  приложение тоже запустится и обработает видео на CPU, но заметно медленнее
  — интерфейс выведет предупреждение, если CUDA не найдена.
- Интернет нужен **только один раз** — для скачивания весов модели YOLO11x и
  весов ReID-модуля трекера при первом запуске. После этого всё работает
  офлайн (веса кэшируются локально Ultralytics/PyTorch).
- **ffmpeg (рекомендуется, необязателен).** Устанавливается отдельно системным
  менеджером пакетов (`sudo apt install ffmpeg` / `brew install ffmpeg` /
  `winget install ffmpeg` на Windows). Без него видео пишется в кодеке OpenCV
  по умолчанию (mp4v), который не во всех браузерах проигрывается через
  встроенный плеер `st.video` — с установленным ffmpeg приложение
  автоматически перекодирует итоговое видео и хайлайты в H.264, который
  проигрывается везде. Без ffmpeg видео всё равно доступно — через кнопку
  «⬇️ Скачать» рядом с каждым плеером.

## Установка (Windows/Linux, RTX 4080)

1. Создайте и активируйте виртуальное окружение:

   ```bash
   python -m venv .venv
   # Windows:
   .venv\Scripts\activate
   # Linux/macOS:
   source .venv/bin/activate
   ```

2. **Сначала** установите CUDA-версию PyTorch/torchvision под вашу версию
   CUDA-драйвера (RTX 4080 поддерживает сборки CUDA 12.x). Проверьте актуальную
   команду на https://pytorch.org/get-started/locally/, например для CUDA 12.4:

   ```bash
   pip install torch==2.4.1 torchvision==0.19.1 --index-url https://download.pytorch.org/whl/cu124
   ```

   Это важно сделать **до** установки `requirements.txt`, иначе pip может
   поставить CPU-сборку torch с обычного PyPI.

3. Установите остальные зависимости:

   ```bash
   pip install -r requirements.txt
   ```

   (pip не переустановит уже стоящие подходящие версии torch/torchvision из
   шага 2, так как версии в `requirements.txt` совпадают — это ожидаемо и
   безопасно).

4. Запустите приложение:

   ```bash
   streamlit run app.py
   ```

   При первом запуске Ultralytics автоматически скачает веса модели
   `yolov11x.pt` (при недоступности — попробует официальный алиас
   `yolo11x.pt`) и веса ReID-модуля трекера — это требует интернет один раз.
   Все последующие запуски полностью офлайн, веса используются из локального
   кэша.

5. Приложение откроется в браузере (обычно `http://localhost:8501`).

## Как пользоваться (пошаговый мастер)

**Шаг 1 — Режим камеры и загрузка видео.** Сначала выберите режим съёмки:

   - **📷 Статичная камера** (по умолчанию) — камера неподвижна, площадка с
     обоими кольцами в кадре. Стандартный, полностью проверенный режим.
   - **🎥 Камера в движении / панорама (экспериментально)** — если оператор
     плавно панорамирует/наклоняет камеру во время съёмки. См. предупреждение
     в GUI и раздел «Как это работает» ниже про ограничения этого режима.

Затем загрузите видеофайл тренировки (MP4/MOV).

**Шаг 2 — Превью и настройка зон.** На слайдере выберите секунду видео для
превью-кадра. Приложение один раз запускает быструю детекцию по этому кадру и
предлагает **авто-откалиброванный** порог владения мячом (под средний размер
игрока на видео — камера близко/далеко не важно), с кнопкой «🔄 Пересчитать по
кадру», если хотите пересчитать на другом кадре.

   - **Клик мышкой по превью** ставит центр выбранного кольца (переключатель
     «Кольцо 1 / Кольцо 2» над картинкой) — окружности сразу перерисовываются.
     Если пакет `streamlit-image-coordinates` не установлен, доступны только
     числовые поля X/Y/радиус ниже (полностью рабочий fallback).
   - На самом превью также рисуется полупрозрачный **круг-подсказка** текущего
     порога владения мячом в реальном масштабе кадра — не нужно гадать по
     числу пикселей.
   - Настраиваются: порог владения мячом (px), максимальное время передачи
     (сек), память мяча при потере детекции (сек), кулдаун события у кольца
     (сек).
   - Тумблер **«🔧 Улучшить качество кадра перед детекцией»** — опциональный
     апскейл + лёгкая резкость перед детекцией, может помочь трекеру на видео
     низкого разрешения (не панацея при изначально плохом видео, замедляет
     обработку).
   - Раздел **«🏀 Детекция мяча и производительность»** — отдельные слайдеры
     порога уверенности для мяча (по умолчанию ниже, т.к. мяч мельче и часто
     смазан при полёте) и для игроков, плюс слайдер разрешения инференса
     `imgsz` (640–1280px, выше — легче разглядеть мелкий мяч, но медленнее).
   - Если включён режим «камера в движении» на шаге 1 — здесь появится
     подсказка о том, что зоны колец заданы относительно ТЕКУЩЕГО кадра
     превью и на шаге 4 будут автоматически «следовать» за панорамой.

   Если на вашем видео передачи не засчитываются — увеличьте порог владения
   и/или окно передачи, и проверьте отладочный таймлайн на шаге 4.

**Шаг 3 — Сопоставление игроков и диагностика мяча.** Приложение запускает
короткое предварительное сканирование первых N секунд видео (тот же трекер,
конфиг и пороги/качество, что и в финальном анализе — чтобы ID совпадали) и
показывает компактную сетку из кропов игроков одинаковой высоты (пропорции
сохраняются) — по одному характерному кропу на каждый найденный ID. Впишите
имя и номер для каждого — поля можно оставить пустыми, тогда в таблице будет
использован плейсхолдер «Игрок {ID}».

Ниже — кнопка **«🔍 Проверить видимость мяча»**: сэмплирует кадры по всему
видео на текущем пороге уверенности мяча и `imgsz` (шаг 2) и показывает,
в каком % кадров модель вообще обнаружила мяч и с какой средней уверенностью
— с явным сообщением, стоит ли менять пороги/качество или проблема в самой
видимости мяча на видео. Рекомендуется запускать перед шагом 4, если раньше
события (броски/передачи) не фиксировались.

**Шаг 4 — Полный анализ.** Нажмите «🚀 Запустить полный анализ» и следите за
прогресс-баром. По завершении появится:
   - таблица **Box Score**: `ID игрока | Имя | Номер | Броски | Попадания | Точность (%) | Сделано передач`;
   - отладочный expander с таймлайном владения мячом — каждая смена
     владельца, засчитана ли передача и почему (или почему нет);
   - полное аннотированное видео обработки (папка `output_videos/`) — с
     кнопкой «⬇️ Скачать», если по какой-то причине оно не проигрывается
     во встроенном плеере браузера;
   - плеер со всеми нарезанными хайлайтами из папки `highlights/` — броски
     (`goal_ring<N>_ID<id>_frame_<N>.mp4`) и передачи
     (`pass_from_ID<A>_to_ID<B>_frame_<N>.mp4`), тоже с кнопками скачивания.

Кнопки «← Назад» / «Далее →» позволяют вернуться на предыдущий шаг (например,
подправить зону кольца) без потери введённых данных; кнопка «🔄 Начать заново
с новым видео» на шаге 4 полностью сбрасывает состояние.

## Как это работает (логика аналитики)

- **Трекинг без номеров.** Конфиг `custom_botsort.yaml` включает
  `with_reid: True` — трекер BoT-SORT удерживает ID игрока по визуальным
  признакам (форма/обувь), а не по номеру, с повышенным `track_buffer` (120
  кадров), чтобы не терять игрока при коротких перекрытиях.
- **Владение мячом.** Игрок считается владеющим мячом, если центр мяча
  находится не дальше настраиваемого порога (диапазон 20–250 px — слайдер на
  шаге 2) от его рамки (0, если центр мяча внутри рамки).
- **Авто-масштабирование порога владения.** Фиксированный порог в пикселях не
  учитывает масштаб конкретного видео — на видео с дальней камерой игрок
  занимает мало пикселей, а порог 90px может оказаться слишком большим
  относительно размера игрока (или наоборот, слишком маленьким на видео
  крупным планом). Поэтому на шаге 2 приложение один раз прогоняет детектор
  по превью-кадру, считает среднюю диагональ рамки игрока и предлагает
  порог ≈ 50% от этой диагонали в качестве дефолта (слайдер остаётся
  полностью настраиваемым вручную).
- **«Память» мяча.** Если детектор не нашёл мяч в текущем кадре (смаз
  движения, блик, частичное перекрытие руками), приложение до
  `ball_memory_seconds` (по умолчанию 0.3 сек, слайдер на шаге 2) продолжает
  считать мяч находящимся в последней известной точке. Без этого короткие
  пропуски детекции ровно в момент приёма пасующим партнёром обрывали цепочку
  владения, и передача никогда не засчитывалась — это и было основной
  причиной нулевых передач на реальном видео.
- **Передача.** Если владелец мяча сменился с игрока А на игрока Б в течение
  настраиваемого окна (по умолчанию до 1.8 сек, диапазон 0.3–3.0 сек) — это
  засчитывается как успешная передача, игроку А добавляется +1 пас. Каждая
  смена владельца (в т.ч. отклонённая из-за слишком долгого перелёта)
  логируется и видна в отладочном таймлайне на шаге 4.
- **Бросок/попадание.** Событие «бросок» фиксируется при входе центра мяча в
  одну из ДВУХ окружностей (центр кольца, радиус), заданных в GUI, с
  кулдауном (по умолчанию 3 сек) для каждого кольца отдельно, чтобы одно и то
  же зависание мяча у кольца не засчитывалось многократно. Бросок
  засчитывается игроку, который последним владел мячом, либо (если владелец
  неизвестен) ближайшему к кольцу игроку. Через долю секунды после входа в
  зону программа анализирует направление полёта мяча (линейная аппроксимация
  вертикальной скорости): если мяч продолжает падать вниз и оказывается ниже
  кольца — засчитывается попадание; если мяч отскочил — попадание не
  засчитывается (бросок при этом всё равно учитывается в статистике попыток).
- **Хайлайты.** Программа постоянно держит в памяти кольцевой буфер кадров
  за последние 5 секунд. При фиксации гола/передачи в файл сохраняются эти
  5 секунд «до» + следующие 2 секунды «после» события — автономный
  MP4-ролик через `cv2.VideoWriter`.
- **Отладочный таймлайн.** На шаге 4 в expander «🔍 Отладка» видна таблица
  всех смен владения мячом (с кем, когда, засчитана передача или отклонена и
  почему) и график владельца мяча во времени — используйте это, чтобы
  подобрать пороги, если события не детектируются на вашем видео.
- **Раздельные пороги уверенности мяч/игроки.** У `model.track()`/
  `model.predict()` в Ultralytics нет параметра per-class confidence, поэтому
  инференс запускается с ЕДИНЫМ низким порогом `conf = min(ball_conf,
  person_conf)` (чтобы не потерять мяч на этапе NMS модели), а затем каждый
  класс фильтруется своим порогом постфактум: детекции `person` с
  уверенностью ниже `person_conf` отбрасываются, детекции `sports ball` ниже
  `ball_conf` — тоже. Это позволяет держать порог для мяча значительно ниже
  (мяч мельче и чаще смазан при полёте), не засоряя при этом список игроков
  ложными низкоуверенными рамками.
- **Разрешение инференса (imgsz).** YOLO перед инференсом letterbox-ит кадр до
  `imgsz` пикселей (дефолт ultralytics — 640). На кадре высокого разрешения
  после уменьшения до 640px мяч может занимать буквально пару пикселей и
  теряться. Слайдер `imgsz` (640–1280px, шаг 2) позволяет увеличить это
  разрешение для более уверенной детекции мелких объектов ценой более
  медленного инференса — особенно заметно на CPU.
- **Диагностика видимости мяча.** `diagnose_ball_visibility()` сэмплирует до
  120 равномерно распределённых по всему видео кадров, на каждом запускает
  `model.predict()` только по классу `sports ball` с текущими `ball_conf`/
  `imgsz`, и считает % кадров с обнаруженным мячом плюс среднюю уверенность.
  Это отдельный от полного трекинга быстрый прогон (без BoT-SORT), поэтому
  выполняется за секунды даже на многоминутном видео, и помогает быстро
  отличить «проблема в порогах» (высокий % при низком пороге) от «мяч в
  принципе плохо виден на этом видео» (низкий % даже на минимальном пороге).
- **Камера в движении (экспериментально).** Если выбран этот режим на шаге 1,
  `estimate_camera_transforms()` проходит по всему видео методом
  оптического потока Лукаса-Канаде (`cv2.calcOpticalFlowPyrLK`) по устойчивым
  фоновым фичам (`cv2.goodFeaturesToTrack`), оценивает покадровую similarity-
  трансформацию (сдвиг + поворот + равномерный масштаб) через
  `cv2.estimateAffinePartial2D` с RANSAC-отбрасыванием выбросов (в т.ч. точек
  на движущихся игроках, которые занимают меньшую часть кадра, чем статичный
  фон) и накапливает эти трансформации от кадра к кадру. На шаге 4
  `compute_dynamic_rings()` использует накопленные трансформации, чтобы для
  каждого кадра пересчитать позицию (и радиус — при лёгком зуме) обеих зон
  колец относительно кадра, на котором пользователь их задал на шаге 2. На
  аннотированном выходном видео пересчитанные зоны рисуются поверх кадра —
  это одновременно и логика события, и визуальное подтверждение того, что
  кольцо действительно «следует» за панорамой. Метод рассчитан на **плавную**
  панораму/наклон камеры; резкий зум, сильная тряска или смена плана (склейка)
  могут сбить накопленную оценку — в этом случае лучше использовать статичный
  режим или переснять видео. Статичный режим (по умолчанию) полностью
  независим от этой логики и не затрагивается.
- **Улучшение качества кадра (опционально).** При включённом тумблере на
  шаге 2 каждый кадр перед подачей в детектор апскейлится (`cv2.resize` с
  `INTER_LANCZOS4`) и слегка резчится (unsharp mask), затем координаты
  найденных боксов пересчитываются обратно в исходное разрешение — итоговое
  видео и хайлайты остаются в исходном разрешении входного файла. Это может
  помочь трекеру/ReID различать игроков на видео низкого разрешения, но не
  является панацеей: если исходное видео изначально сильно сжато или
  размыто, апскейл не восстановит потерянные детали, а лишь немного
  стабилизирует детекцию.
- **Воспроизведение видео в браузере.** `cv2.VideoWriter` по умолчанию
  использует кодек mp4v (MPEG-4 Part 2), который валиден как файл, но не
  всегда проигрывается через HTML5 `<video>` (а именно так работает
  `st.video` в Streamlit) — файл при этом открывается внешними плеерами, но
  выглядит «битым» во встроенном плеере страницы. Если в системе установлен
  `ffmpeg`, после записи итоговое видео и каждый хайлайт автоматически
  перекодируются в H.264 (`yuv420p`, `+faststart`), который проигрывается
  практически везде. Если `ffmpeg` не найден — файл остаётся как есть, GUI
  показывает предупреждение и рядом с каждым плеером всегда доступна кнопка
  «⬇️ Скачать», чтобы посмотреть видео внешним плеером.

## Ограничения и заметки

- Сопоставление ID → имя/номер (шаг 3) основано на быстром сканировании
  первых секунд видео тем же трекером и конфигом, с явным сбросом состояния
  трекера перед каждым независимым прогоном — это максимизирует совпадение ID
  с финальным анализом на шаге 4, но 100% гарантии в редких случаях (например,
  если игрок отсутствовал в кадре во время сканирования) дать не может.
  Если ID разошлись — можно пересканировать или пропустить сопоставление.
- Детекция «попадание/промах» — упрощённая математическая эвристика по 2D
  траектории мяча (без депроекции в 3D), рассчитанная на статичную камеру с
  видом на кольцо. Для нестандартных ракурсов может потребоваться подстройка
  координат/радиуса зоны кольца.
- Приложение рассчитано в первую очередь на одну статичную камеру, снимающую
  всю площадку. Экспериментальный режим «камера в движении» (панорама)
  помогает при плавном панорамировании/наклоне, но НЕ рассчитан на резкий
  зум, сильную тряску камеры (например, съёмка с рук без стабилизации) или
  смену плана/склейку — в этих случаях оценка сдвига камеры может накопить
  ошибку, и виртуальные зоны колец начнут расходиться с реальными кольцами на
  площадке. Если это происходит — переключитесь на статичный режим камеры
  (штатив/крепление) или переснимите видео плавнее.
- Клик мышкой по превью для выбора центра кольца требует пакет
  `streamlit-image-coordinates` (уже в `requirements.txt`). Если по какой-то
  причине он не установлен, приложение не падает — просто скрывает
  кликабельный режим и оставляет числовые поля X/Y/радиус как единственный
  способ настройки зон.
- Если CUDA/GPU или интернет для первого скачивания весов недоступны,
  приложение не «падает» — оно покажет предупреждение в интерфейсе и
  продолжит работу (на CPU, либо в демонстрационном режиме без реального
  распознавания, если веса модели не удалось загрузить). Это защитное
  поведение для сред без GPU/интернета; на рабочей машине с RTX 4080 вся
  боевая логика трекинга и аналитики используется полноценно.

## Разработка

Быстрая проверка синтаксиса без запуска (не требует GPU/интернета/весов
модели):

```bash
python -m py_compile app.py
```
